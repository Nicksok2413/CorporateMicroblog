# Сервис Корпоративных Микроблогов (Backend) 🚀

![Python](https://img.shields.io/badge/Python-3.12-blueviolet) ![FastAPI](https://img.shields.io/badge/FastAPI-0.115-success) ![SQLAlchemy](https://img.shields.io/badge/SQLAlchemy-2.0-orange) 
![PostgreSQL](https://img.shields.io/badge/PostgreSQL-17-blue) ![Nginx](https://img.shields.io/badge/Nginx-stable-brightgreen)

Бэкенд для корпоративного сервиса микроблогов, реализованный на Python с использованием FastAPI, SQLAlchemy и PostgreSQL.

## 📝 Обзор

Этот проект представляет собой бэкенд-часть корпоративного сервиса микроблогов. Пользователи могут публиковать короткие сообщения ("твиты"), подписываться на других пользователей, ставить лайки и просматривать ленту новостей от тех, на кого они подписаны.

**Архитектура:**
*   **Nginx** (`nginx` сервис) выступает в роли входной точки (реверс-прокси), слушает порт 8000 (по умолчанию). Он отвечает за:
    *   Раздачу статических файлов пользовательского интерфейса (из `src/static/`).
    *   Раздачу загруженных медиафайлов (изображений) по пути `/media/`.
    *   Проксирование всех API-запросов (начинающихся с `/api/`) на бэкенд-сервис FastAPI.
*   **FastAPI** (`web` сервис) обрабатывает исключительно API-запросы (`/api/...`), взаимодействуя с базой данных PostgreSQL через SQLAlchemy и Alembic.
*   **PostgreSQL** (`db` сервис) хранит все данные приложения.
*   Аутентификация пользователей предполагается внешней, бэкенд работает с предоставленным `api-key` в заголовке запроса к API.
Для повышения безопасности API-ключи пользователей хранятся в базе данных в хешированном виде (Argon2 + SHA256). Дополнительно, на уровне Nginx настроено ограничение частоты запросов (Rate Limiting) для защиты от подбора ключей.


## ✨ Основные Возможности API (`/api/...`)

*   **Твиты:**
    *   Создание нового твита (с текстом и опциональными изображениями).
    *   Удаление *своего* твита (связанные медиафайлы также удаляются).
*   **Лайки:**
    *   Постановка отметки "Нравится" твиту.
    *   Снятие отметки "Нравится".
*   **Подписки:**
    *   Подписка на другого пользователя.
    *   Отписка от другого пользователя.
*   **Лента:**
    *   Получение персонализированной ленты твитов от пользователей, на которых подписан текущий пользователь, и своих собственных твитов.
    *   Сортировка ленты по популярности (количеству лайков).
*   **Профили:**
    *   Получение информации о своем профиле (подписчики, подписки).
    *   Получение информации о профиле другого пользователя по ID.
*   **Медиа:**
    *   Загрузка изображений (`POST /api/medias`) для последующего прикрепления к твитам.

## 🛠️ Технологический стек

*   **Язык:** Python 3.12
*   **Веб-сервер/Прокси:** Nginx
*   **Фреймворк:** FastAPI
*   **База данных:** PostgreSQL 17
*   **ORM:** SQLAlchemy 2.0 (async)
*   **Драйвер БД:** psycopg3 (binary)
*   **Миграции:** Alembic
*   **Валидация данных:** Pydantic V2
*   **Логирование:** Loguru
*   **Тестирование:** Pytest, pytest-asyncio, pytest-cov, httpx
*   **Линтинг/Форматирование:** Ruff
*   **Типизация:** Mypy
*   **Контейнеризация:** Docker, Docker Compose
*   **ASGI сервер:** Uvicorn

## 📂 Структура Проекта

```
├── alembic/               # Alembic миграции БД
├── docker/                # Конфигурация и Dockerfiles для сервисов
│   ├── nginx/
│   │   ├── Dockerfile     # Dockerfile для Nginx
│   │   └── nginx.conf     # Конфигурация Nginx
│   └── web/
│       ├── Dockerfile     # Dockerfile для FastAPI приложения
│       └── entrypoint.sh  # Скрипт точки входа web сервиса
├── sample_media/          # Демо медиа-файлы для демонстрации работы с изображениями
├── src/                   # Исходный код FastAPI приложения
│   ├── api/               # API слой (роуты, зависимости)
│   ├── core/              # Ядро (конфигурация, БД, исключения, логирование)
│   ├── models/            # Модели SQLAlchemy
│   ├── repositories/      # Репозитории (слой доступа к данным)
│   ├── schemas/           # Схемы Pydantic (валидация, сериализация)
│   ├── services/          # Сервисы (бизнес-логика)
│   ├── static/            # Статика фронтенда (раздается Nginx)
│   └── main.py            # Точка входа FastAPI приложения
├── tests/                 # Тесты (unit, integration)
│   ├── conftest.py        # Общие фикстуры для всех тестов (напр., объекты моделей)
│   ├── integration/       # Интеграционные тесты
│   │   ├── api/           # Интеграционные тесты API
│   │   ├── core/          # Интеграционные тесты ядра приложения (Ошибки)
│   │   └── conftest.py    # Фикстуры для integration тестов (БД, HTTP клиент и др.)
│   └── unit/              # Unit-тесты
│       ├── core/          # Unit-тесты ядра приложения (Сессии и настройки конфигурации)
│       ├── dependencies/  # Unit-тесты зависимостей
│       ├── repositories/  # Unit-тесты репозиториев
│       ├── routes/        # Unit-тесты функций-обработчиков роутов
│       ├── services/      # Unit-тесты сервисов (бизнес-логика)
│       └── conftest.py    # Фикстуры для unit тестов (моки)
├── .env.example           # Шаблон файла переменных окружения
├── .gitignore             # Игнорируемые файлы Git
├── .dockerignore          # Игнорируемые файлы Docker при сборке
├── alembic.ini            # Конфигурация Alembic
├── docker-compose.yml     # Docker Compose конфигурация (основной оркестратор)
├── pytest.ini             # Конфигурация Pytest
├── requirements.txt       # Зависимости для Production
├── requirements-dev.txt   # Зависимости для разработки и тестов
└── README.md              # Этот файл
```

## 🚀 Установка и Запуск

Для запуска проекта вам понадобятся **Docker** и **Docker Compose**.

1.  **Клонируйте репозиторий:**
    ```bash
    git clone https://github.com/Nicksok2413/CorporateMicroblog
    cd CorporateMicroblog
    ```

2.  **Создайте файл переменных окружения:**
    Скопируйте файл `.env.example` в `.env`:
    ```bash
    cp .env.example .env
    ```
    **Отредактируйте `.env`**, указав необходимые значения, как минимум пароль для PostgreSQL (`POSTGRES_PASSWORD`). 
    Режимы `TESTING` и `DEBUG` должны быть `False` для production.


3.  **Сгенерируйте SSL-сертификат для HTTPS (только для локальной разработки):**
    Если папки `docker/nginx/certs` еще нет, создайте её: `mkdir -p docker/nginx/certs`. Затем выполните команду в корне проекта:
    ```bash
    openssl req -x509 -nodes -days 365 -newkey rsa:2048 \
        -keyout docker/nginx/certs/nginx-selfsigned.key \
        -out docker/nginx/certs/nginx-selfsigned.crt \
        -subj "/C=RU/ST=Moscow/L=Moscow/O=MyCompany/OU=IT/CN=localhost"
    ```
    *Примечание: Файлы `.key` и `.crt` добавлены в `.gitignore` и не должны попадать в репозиторий.*


4.  **Запустите с помощью Docker Compose:**
    Эта команда соберет образы (если необходимо) и запустит три контейнера: `db` (PostgreSQL), `web` (FastAPI приложение) и `nginx` (реверс-прокси и сервер статики). Nginx будет слушать порт **8000** на вашем хосте и перенаправлять запросы соответствующим сервисам. Миграции базы данных будут применены автоматически при старте сервиса `web`.
    ```bash
    docker-compose up -d --build
    ```
    *   Флаг `-d` запускает контейнеры в фоновом режиме.
    *   Флаг `--build` принудительно пересобирает образы (рекомендуется при первом запуске или после изменений в `Dockerfile` или коде).
    *   При первом запуске может потребоваться некоторое время на загрузку образа PostgreSQL и сборку образа приложения.    


5.  **Проверка:**
    *   Веб-интерфейс будет доступен по адресу `http://localhost:8000`.
    *   Документация API (Swagger UI) доступна по адресу `http://localhost:8000/docs`.
    *   Альтернативная документация (ReDoc) доступна по адресу `http://localhost:8000/redoc`.


6.  **Остановка:**
    Для остановки контейнеров выполните:
    ```bash
    docker-compose down
    ```
    Чтобы остановить и удалить тома (volumes) с данными PostgreSQL, медиа и логами (например, для полного сброса):
    ```bash
    docker-compose down -v
    ```

## 🔑 Использование API

### Аутентификация

Все API-эндпоинты (начинающиеся с `/api/...`) требуют передачи API-ключа пользователя в HTTP-заголовке `api-key`.

При использовании UI (`/`):

1.  В правом верхнем углу форма для ввода API-ключа.
2.  В поле `api-key текущего пользователя:` по умолчанию стоит API-ключ тестового пользователя `Nick`: `test`.
3.  Для входа под другим пользователем введите API-ключ одного из тестовых пользователей (см. ниже) и нажмите `Установить новый api-key`.

При использовании `curl` или других клиентов, добавьте заголовок:
```bash
curl -X GET "http://localhost:8000/api/users/me" -H "accept: application/json" -H "api-key: test"
```

### Тестовые Пользователи (из начальных данных)

При первом запуске с применением миграций создаются следующие тестовые пользователи:

| Имя                 | ID | API Ключ (`api-key`) | Примечание                   |
|:--------------------|:--:| :------------------- |:-----------------------------|
| Nick                | 1  | `test`               | Имеет твиты, подписки, лайки |
| Alice               | 2  | `alice_key`          | Имеет твиты, подписки, лайки |
| Bob                 | 3  | `bob_key`            | Имеет твиты, подписки, лайки |
| Charlie             | 4  | `charlie_key`        | Имеет твиты, подписки, лайки |
| David (no tweets)   | 5  | `david_key`          | Без твитов                   |

Используйте соответствующие значения из колонки "API Ключ" для аутентификации от имени нужного пользователя.

## ✅ Тестирование

Для запуска тестов необходимо установить зависимости для разработки и сам `pytest`. Рекомендуется делать это в виртуальном окружении.

1.  **Создайте и активируйте виртуальное окружение (если еще нет):**
    ```bash
    python -m venv .venv
    source .venv/bin/activate  # Linux/macOS
    # .venv\Scripts\activate  # Windows
    ```

2.  **Установите зависимости:**
    ```bash
    pip install -r requirements.txt -r requirements-dev.txt
    ```

3. Отредактируйте `.env`, указав `TESTING`=`True`. 
   Для более подробного логирования можно включить режим отладки: `DEBUG`=`True`.

4.  **Запустите тесты:**
    ```bash
    pytest
    ```
    Эта команда запустит все тесты (unit и integration). Тесты используют отдельную базу данных SQLite в памяти и не требуют запущенного Docker Compose.

5.  **Запуск тестов с отчетом о покрытии:**
    ```bash
    pytest --cov=src --cov-report term-missing -vv
    ```

## 🧹 Линтинг и Форматирование

Для проверки и форматирования кода используется `Ruff`. 
Для проверки типов используется `mypy`.
Запускайте в активном виртуальном окружении.

1.  **Проверка:**
    ```bash
    ruff check .
    ```
2.  **Проверка + Автоисправление:**
    ```bash
    ruff check . --fix
    ```

3.  **Проверка форматирования:**
    ```bash
    ruff format . --check
    ```

4.  **Автоисправление форматирования:**
    ```bash
    ruff format .
    ```

5.  **Проверка типов с `mypy`:**
    ```bash
    mypy src
    ```

## 💾 Миграции Базы Данных

Для управления схемой базы данных используется `Alembic`.

*   **Применение миграций:** Автоматически при старте контейнера `web` с помощью `entrypoint.sh`. Вручную: `alembic upgrade head`.
*   **Создание новой миграции (при изменении моделей SQLAlchemy):**
    ```bash
    # Убедитесь, что сервис БД запущен (docker-compose up -d db)
    # Выполняйте в активном venv с установленными зависимостями
    alembic revision --autogenerate -m "Краткое описание изменений"
    ```
    После генерации **внимательно просмотрите** созданный файл миграции в `alembic/versions/` перед его применением.
*   **Применение миграций вручную (если нужно):**
    ```bash
    # Применить все до последней
    alembic upgrade head
    # Откатить последнюю миграцию
    alembic downgrade -1
    # Посмотреть историю
    alembic history
    ```

---

Спасибо за использование сервиса!
